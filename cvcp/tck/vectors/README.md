# Canonical Test Vectors

This directory contains the canonical protocol test vectors for CVCP.

- `valid/`: Fully compliant protocol artifacts.
- `invalid/`: Malformed or non-conforming inputs.
- `canonical/`: Byte-for-byte deterministic serialization references.
- `negotiation/`: RFC-0014 capability negotiation scenarios.
